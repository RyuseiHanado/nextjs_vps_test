name: CD
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install SSH Key for Deploy
        uses: appleboy/ssh-action@master
        with:
          key: ${{ secrets.XSERVER_VPS_SSH_KEY }}
          host: ${{ secrets.XSERVER_VPS_HOST_NAME }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.XSERVER_VPS_SSH_PORT }}
          script: |
            cd /var/git/nextjs_vps_test/
            git pull
            cp -r /var/git/nextjs_vps_test/build/* /var/www/nextjs_vps_test
